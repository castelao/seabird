header: |
    (?P<header>
        (
            \*\ .*\n  # Antything starting with *
        )+
    )
    (?P<headerblob>
        (
            \*\* .*\n  # Antything starting with *
        )+
    )
    (?P<descriptors>
        (
            \#.*\n  # Antything
        )+
    )

sep: |
    \*END\*\r\n

data: |
  (?P<data>
    (?:
        (?:                     # Start of one variable
          \ *
          -?                    # Can be a negative number
          \d+                   # Any digits
            (?:
              \.\d+               # Real numbers
              (?:
                e[\+|-]\d+        # Cientific notation
              )?
            )?
        )+                      # Repeat as much variables as there are
        #  (?:
        #  )?
          \s*                   # Following empty spaces, end of line
    )+                          # One or more lines
  )

descriptors:
    nquan: |
      \#\ nquan\ =\ (\d+)
    nvalues: |
      \#\ nvalues\ =\ (\d+)
    start_time: |
      \#\ start_time\ =\ (.*)\r?\n      # Should work, but I'm not happy with this.
    bad_flag: |
      \#\ bad_flag\ =\ (.*?)\s

#descriptors: |
#    \#\ nquan\ =\ (?P<nquan>\d+) \s*
#    \#\ nvalues\ =\ (?P<nvalues>\d+) \s*
#    \#\ units\ =\ \w*\s*
#    (?P<names>
#      (
#      \#\ name\ \d+\ =\ (?P<name>\w+) #\: Time, Elapsed [seconds]
#      .*
#      \s*
#      )+
#    )

names: |
  \#\ name\ (?P<id>\d+)\ =\ (?P<name>\w+)\:\ (?P<longname>.*)\r\s


latitude: |
    # [LAT|LATITUDE]
    (?P<degree>\d{2})
    \ 
    (?P<minute>\d{2}.\d{2,3})
    \ *[N|n|S|s]
    #- "(\\d{2} \\d{2}\\.\\d{2}\\s+[sn])"
    #- |
    #    \d{2}\                  # Test

longitude: |
    # [LON|LONG|LONGITUDe]
    (?P<degree>\d{3})
    \ 
    (?P<minute>\d{2}.\d{2,3})
    \ *[W|w|E|e]
